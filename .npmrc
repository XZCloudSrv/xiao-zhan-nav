# .npmrc
# 确保使用 exact 版本，避免版本冲突
save-exact=true
engine-strict=true
legacy-peer-deps=false
fund=false
progress=true

# 国内用户可以使用镜像加速
# registry=https://registry.npmmirror.com/
# sass_binary_site=https://npmmirror.com/mirrors/node-sass/

# 如果遇到网络问题，可以设置超时
fetch-retry-maxtimeout=60000
fetch-retries=5